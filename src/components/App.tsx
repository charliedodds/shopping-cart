import { BrowserRouter } from 'react-router-dom';

import ShopApp from './ShopApp';

const App = () => {
  return (
    <BrowserRouter>
      <ShopApp />
    </BrowserRouter>
  );
};

export default App;
